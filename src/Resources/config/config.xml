<?xml version="1.0" encoding="UTF-8"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/shopware/platform/trunk/src/Core/System/SystemConfig/Schema/config.xsd">
    <card>
        <title>AxiTrace Tracking</title>
        <title lang="de-DE">AxiTrace Tracking</title>

        <input-field type="bool">
            <name>enabled</name>
            <label>Enabled</label>
            <label lang="de-DE">Aktiviert</label>
            <defaultValue>true</defaultValue>
            <helpText>Master switch — when off, the plugin captures no events.</helpText>
            <helpText lang="de-DE">Hauptschalter — wenn deaktiviert, erfasst das Plugin keine Ereignisse.</helpText>
        </input-field>

        <input-field type="password">
            <name>publicKey</name>
            <label>AxiTrace Public Key (pk_live_...)</label>
            <label lang="de-DE">AxiTrace Public Key (pk_live_...)</label>
            <helpText>Find this in axitrace.com → Workspace → Keys. Stored encrypted at rest.</helpText>
            <helpText lang="de-DE">Zu finden unter axitrace.com → Workspace → Keys. Wird verschlüsselt gespeichert.</helpText>
        </input-field>

        <input-field type="text">
            <name>trackingDomain</name>
            <label>Custom tracking domain (optional)</label>
            <label lang="de-DE">Eigene Tracking-Domain (optional)</label>
            <placeholder>pixel.yourstore.com</placeholder>
            <helpText>Optional: enter your own subdomain (e.g. pixel.yourstore.com) that you have CNAME'd to stat.axitrace.com. The browser SDK will then load from your domain, placing cookies as first-party for improved tracking accuracy on iOS/Safari. Leave empty to use stat.axitrace.com (works for everyone).</helpText>
            <helpText lang="de-DE">Optional: Geben Sie Ihre eigene Subdomain ein (z. B. pixel.ihrshop.de), die per CNAME auf stat.axitrace.com verweist. Das Browser-SDK wird dann von Ihrer Domain geladen, sodass Cookies als First-Party gesetzt werden — verbessert das Tracking auf iOS/Safari. Leer lassen, um stat.axitrace.com zu verwenden (funktioniert für alle).</helpText>
        </input-field>

        <input-field type="single-select">
            <name>conversionValueBasis</name>
            <label>Conversion value</label>
            <label lang="de-DE">Conversion-Wert</label>
            <defaultValue>gross_total</defaultValue>
            <options>
                <option>
                    <id>gross_total</id>
                    <name>Order total incl. VAT and shipping (default)</name>
                    <name lang="de-DE">Bestellsumme inkl. MwSt. und Versand (Standard)</name>
                </option>
                <option>
                    <id>gross_excl_shipping</id>
                    <name>Order total incl. VAT, excl. shipping</name>
                    <name lang="de-DE">Bestellsumme inkl. MwSt., ohne Versand</name>
                </option>
                <option>
                    <id>net_total</id>
                    <name>Order total excl. VAT, incl. shipping</name>
                    <name lang="de-DE">Bestellsumme ohne MwSt., inkl. Versand</name>
                </option>
                <option>
                    <id>net_excl_shipping</id>
                    <name>Product revenue only — excl. VAT and shipping</name>
                    <name lang="de-DE">Nur Produktumsatz — ohne MwSt. und Versand</name>
                </option>
            </options>
            <helpText>Which amount is sent as the purchase value to Facebook, Google Ads, GA4, TikTok and Reddit. Applies to new orders only; the gross VAT and shipping amounts are always sent alongside for reference.</helpText>
            <helpText lang="de-DE">Welcher Betrag als Kaufwert an Facebook, Google Ads, GA4, TikTok und Reddit gesendet wird. Gilt nur für neue Bestellungen; MwSt.- und Versandbetrag werden zusätzlich immer mitgesendet.</helpText>
        </input-field>

        <input-field type="bool">
            <name>debugMode</name>
            <label>Debug mode</label>
            <label lang="de-DE">Debug-Modus</label>
            <defaultValue>false</defaultValue>
            <helpText>Verbose SDK console logs in browser. Disable in production.</helpText>
            <helpText lang="de-DE">Ausführliche SDK-Konsolenausgabe im Browser. In der Produktion deaktivieren.</helpText>
        </input-field>
    </card>
</config>
